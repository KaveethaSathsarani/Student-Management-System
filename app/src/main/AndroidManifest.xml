<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.studentmanagementsystem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AdminExam">
        </activity>
        <activity android:name=".Splash"></activity>
        <activity
            android:name=".Admin_Profile"
            android:label="@string/title_activity_admin__profile"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".AdminMenu"
            android:label="@string/app_name" />
        <activity android:name=".MainClass" />
        <activity
            android:name=".login1"
            android:label="Study Lab"
            android:logo="@drawable/logo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>


        </activity>
        
        <activity
            android:name=".AdminExamView"
            android:label="@string/title_activity_my">

        </activity>
        <activity
            android:name=".AdminExamInsertUpdate"
            android:label="@string/title_activity_about"></activity>
        <activity
            android:name=".AdminExamDelete"
            android:label="@string/title_activity_contact"></activity>
    </application>

</manifest>